Адрес, который мы хотим пингануть захардкожен внутри функции main

для компиляции выполняем команду

```
g++ -o ping ./Ping.cpp
```

Для проверки работоспособности запуск производится от имени администратора

```
sudo ./ping
```

