 Для компиляции исполняемого файла используется команда

```
g++ -o webServer webServer.cpp
```

Для запуска выплняем команду

```
./webServer
```

Для отправки запроса переходим по ссылке

http://localhost:8081/имя_файла
